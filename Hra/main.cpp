#include <iostream>

#include "GameEngine.h"
#include "Map.h"

int main() {
    auto* engine = new GameEngine();
/*
    auto* map = new Map(6, 6);


    auto* farm = new Tile("X");
    auto* lumberj = new Tile("L");
    auto* quarry = new Tile("Q");
    auto* house = new Tile("H");

    farm->setAppearance("F");

    std::cout << "Map before changes:\n";
    map->show();

    std::cout << "\nMap after changes:\n";
    map->setValue(5, 3, farm);
    map->setValue(5, 1, lumberj);
    map->setValue(5, 5, quarry);
    map->setValue(1, 4, house);

    map->show();

    delete map; delete farm; delete lumberj;
    delete quarry; delete house;
    return 0;
*/
}
